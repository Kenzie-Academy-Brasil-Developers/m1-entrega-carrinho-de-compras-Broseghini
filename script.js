let produtosCarrinho = document.querySelector(".produtos")

let products = []

function somaDosProdutos(lista){
   
    for(let i = 0; i < lista.length; i++){
   
        i + i
   
    }
}
somaDosProdutos()

    let btnComprar  = document.createElement("button")

    if(id != undefined){

        btnComprar.id =  id
    }

    
    btnComprar.innerText = "Finalizar Compras"


body.appendChild(main)
main.appendChild(productsList)
main.appendChild(productsDetails)

